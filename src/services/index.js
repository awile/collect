
import * as labelService from './label';
import * as photoService from './photo';
import * as photoLabelService from './photoLabel';

export const LabelService = { ...labelService };
export const PhotoService = { ...photoService };
export const PhotoLabelService = { ...photoLabelService };
